"""multiagents — delegate work to other agent CLIs as supervised subagents.

Exposes opencode and Antigravity (``agy``) to Claude Code through MCP, with git
worktree isolation, a shared agent tree, stream-level supervision, and
quota-aware routing.
"""

__version__ = "0.1.0"
